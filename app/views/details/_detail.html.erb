<tr>
  <td><%=detail.name %></td>
  <td><%=detail.email %></td>
  <td><%= link_to 'delete', detail, method: :delete, data: { confirm: 'Are you sure?'}, remote: true, class: 'delete' %></td>
  <td><%= link_to 'Edit', edit_detail_path(detail), remote:true, class: 'edit' %></td>

</tr>